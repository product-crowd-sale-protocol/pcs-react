```
import { Setting, THEME, EOS_NETWORK } from "pcs-react"

<Setting theme={THEME.DARK} appName={"YOUR_APP_NAME"} network={EOS_NETWORK.kylin.asia} />
```

```
import { Transfer, THEME, EOS_NETWORK } from "pcs-react"

<Transfer theme={THEME.DARK} appName={"YOUR_APP_NAME"} network={EOS_NETWORK.kylin.asia} />
```

```
import { Password, THEME, EOS_NETWORK } from "pcs-react"

<Password theme={THEME.DARK} appName={"YOUR_APP_NAME"} network={EOS_NETWORK.kylin.asia} />
```

```
import { Scatter, THEME, EOS_NETWORK } from "pcs-react"

<Scatter theme={THEME.DARK} appName={"YOUR_APP_NAME"} network={EOS_NETWORK.kylin.asia} />
```